spring.datasource.url=jdbc:mysql://localhost/vidasonora?createDatabaseIfNotExist=true&serverTimezone=UTC
spring.datasource.username=${dbusername:123456}
spring.datasource.password=${dbPasswordDev:admin}

#spring.jpa.generate-ddl=true
#spring.jpa.hibernate.ddl-auto=validate

spring.flyway.locations=classpath:db/migration

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

security.oauth2.client.scope=password
security.oauth2.client.client-id=${client-id}
security.oauth2.client.client-secret=${cliente-secret}

gestao.jwt.secret:${jwt-secret}